var myArray = ['Mangoes', 'Avacados', 'PeanutButter', 'AlmondButter']; // Declaring an array

var maxLength = 6; // Declaring maxlength 

var longArray = new Array(); // Declaring a new array longArray
{
	for ( var i = 0, myArray[0] >= maxLength, myArray + 1) // Checking the length of each element in an array.
	return	myArray.push(longArray[0]); // Inserting each value inside the new array.
};

