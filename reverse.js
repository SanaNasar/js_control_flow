// Reverse A String 

// //Write a program that will take a hard coded string and reverse it.


function reverse(s) {   // Declaring a function
  var inputString = "building"; 
  for (var i = s.length - 1, j = 0; i >= 0; i--, j++)
  return reverse(inputString); // Using 'for' loop 
}
